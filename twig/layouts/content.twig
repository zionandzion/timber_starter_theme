<div class="container">
	<div class="row">
		<div class="col-md-12">
			<article id="site-content">

				{% block content %}
					<article>
						{% for post in posts %}

							{% block title %}
								<header>
									<h1>{{ post.post_title }}</h1>
									<time datetime="{{ post.post_date_gmt }}" pubdate>{{ post.post_date }}</time>
								</header>
							{% endblock %}

							{% block body %}
								{{ post.content }}
							{% endblock %}

							{% block footer %}
								<footer>
								</footer>
							{% endblock %}

						{% endfor %}
					</article>
				{% endblock %}

			</article>
		</div>
	</div>
</div>